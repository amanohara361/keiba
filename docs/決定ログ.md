# 決定ログ

**新しいセッションを始めたら、CLAUDE.md の次にこれを読む。**

会話で決めたことは会話が終われば消える。ここに残すのは「なぜそうしたか」で、
コードを読めば分かる「何をしたか」は書かない。理由が消えると、次のセッションが
同じ議論をもう一度やるか、理由を知らずに元に戻してしまう。

新しい決定は下に追記する。覆すときは行を消さず、取り消し線で残して理由を書く。

---

## データ取得

**netkeiba のオッズAPIは `action=update` を使う。`init` は古いキャッシュを返す。**
`jra_bias.py` のコメントに元から書いてあった。`init` で取った値は朝の
スナップショットで、直前検算の意味が消える。

**発走前は netkeiba API、発走後はスポーツナビ。判定は「発走時刻を過ぎたか」の一点。**
以前あった「確定オッズは発走から約5時間保持される」は 2026-08-02 に反証済み。
発走2.5時間後に `middle` が返った例があり、経過時間としきい値に相関がない。
時間で分岐する設計は成り立たない。

**Claude のサンドボックスは db.netkeiba.com に到達できない（403 Tunnel connection failed）。
GitHub Actions のランナーは到達できる。**
だからデータ取得は Actions 側に置く。サンドボックスで動かないコードを書いても
テストできないので、`fetcher` を差し替えられる形にしてオフラインでテストする。

**`api.github.com` はサンドボックスから遮断されているが、github.com の git
エンドポイントは通る**（`git ls-remote` は成功する）。読めるが書けない。

**地方（keiba.go.jp）はセッションから到達できる。netkeiba だけが遮断されている。**
2026-08-12 に実測。`RaceDataDownload?type=daily` が 200 / application/zip を返し、
中の CSV がそのまま読めた。`race.netkeiba.com` `db.netkeiba.com` は同じ環境で 403。
**「外部は全部 Actions 経由」と一般化していたのは誤りだった。** 遮断はホスト単位で、
確かめずに広げると、使える経路を自分で塞ぐ。

**ページ構造は推測せず、Actions の probe で実物を見る。**
サンドボックスから netkeiba に到達できないので、パーサーを手元で検証できない。
`probe` は結果を `data/probe/last.txt` にコミットするので、`git pull` して読む
（実行ログのAPIは巨大なJSONに包まれて返ってくる）。推測で正規表現をいじると
当たるまで何往復もする。2026-08-11 の調査で次が判明した。

- 馬ページは3つに分かれている。トップ（`/horse/{id}/`）には競走成績も血統も無い。
  競走成績は `/horse/result/{id}/`、血統は `/horse/ped/{id}/`。
  **`form.py` はトップから探していたため、道悪実績の照会は本番でずっと空だった。**
- 競走成績の見出しは1文字ずつ空白で割れている（「着 順」「オ ッ ズ」）。
  空白を詰めてから完全一致で引く。「馬 場」と「馬場 指数」が隣接するので、
  前方一致にすると取り違える。
- 血統表は5代分を rowspan で結合している。**rowspan の大きさが世代**なので
  それだけを使う。リンクの出現順は深さ優先で父系を潜るため、4つ目が母にならない。
- レース一覧のクラス表示アイコンは `Icon_GradePos01` が付いた方。
  **16〜18は重賞ではなくクラス（3勝・2勝・1勝）**。番号の意味を推測して
  G1/G2/G3に割り当てたため、浜名湖特別（2勝クラス）がG2に化けて
  1次スクリーニングを素通りした。知らない番号は None にする。

---

## 実行と配信

~~**直前検算は「規律クリア」のメールを送らない（2026-08-12 ユーザー承認済み）。**~~
~~地方は1日最大4回（14:07/16:07/18:37/19:37）検算が回るため、問題が無い日でも~~
~~「自動配信 直前検算 完了」が最大4通届き、多すぎるという指摘を受けた。~~
~~`docs/index.html`（GitHub Pages）で最新状態はいつでも見られるようになったため、~~
~~メールは「発注を止めた（BLOCK）」ときだけに絞った。~~
2026-08-13、これが裏目に出た。定時実行が最大92分遅れる仕様（下記）と
「規律クリアは無音」が重なると、メールが来ないことが「まだ実行前／遅延中」
「規律クリアで問題なし」「本当に壊れている」のどれなのか受信側で区別できず、
本当は正常に遅延して動いていただけの回を「怪しい」と誤認させてしまった。
実際に起きた流れ：14:07予定の便が92分遅れて15:39に着火→内容は規律クリア→
無音のまま→「メールが来ない、壊れているのでは」という問い合わせになった。

**直前検算はメールを毎回送るが、規律クリアの回は1行サマリにする
（2026-08-13 ユーザー承認済み）。** 完全な無音（旧方式）と、毎回フルレポート
（さらに旧方式・「多すぎる」の原因）の中間を取った。フルレポートを見たい
ときは `docs/index.html` を見ればよい。「朝の買い目が届いていません」の
警報（買い目ファイルが無い日）は今までどおり毎回フルで送る——こちらは
2026-08-01に13時間気づけなかった事象そのものの検知なので、絞らない。
実行そのものの回数（cron）は変えていない。実オッズの再取得と `docs/index.html` の
更新は毎回のまま。

**定時実行（`schedule`）は大幅に遅れる。手動実行（`workflow_dispatch`）は遅れない。**
実測で85分と119分。毎時00分は特に混む。だから直前検算は前倒しして :07 に置く。
間に合わせたい日は Actions 画面から手動実行する。

**定時実行にしか通らない枝をワークフローに作らない。**
`if: github.event.schedule != ''` で対象日を決めていたが、それだと手動実行の
テストランでその枝を通れず、本番の経路に一度も動かしたことのないコードが残る。
対象日の決定は `card.py` に持たせ、定時実行と手動実行が同じ道を通るようにした。

**終了コードは「ジョブが役目を果たせたか」で決める。**
「買い目が無い」「発注を止めた」は検知して知らせた時点で成功。これを赤にすると
買い目を出さない日が毎回赤くなり、本当の障害（SMTP断・netkeibaの構造変更）が
同じ色に埋もれる。赤は「知らせられなかった」だけに絞る。
0=正常 / 3=要確認だが通知できた / 1=知らせられなかった。

**小さく確実な出力を先にやる。**
2026-08-01、予想もExcelも07:21に完了していたのに、次のDrive投稿でパースエラーが
起きて20:30まで停止した。分析ではなく配信の一手で13時間を失った。
以来、買い目txt → JSON → Drive → Gmail の順で、倒れても手元に残る順に並べている。

**取得できなかったことを静かに「0件」と出さない。**
週次レビューは結果が1件も取れなければ終了コード3を返し、本文の先頭に警告を出す。
空の集計はいちばん危ない出力で、壊れているのに正常に見える。

**朝の予想セッションは Routine（定期トリガー）で起こす。GitHub Actions ではない。**
カード作成（前夜）も直前検算（昼）も週次レビュー（月曜）も無人で回るのに、
その真ん中の「印を打つ」だけが人の起動待ちだった。**鎖が一箇所でも人に戻ると、
その人が起動しなかった週は全部が止まる**（2026-08-01 の13時間と同じ形）。
印は判断なので Actions には置けず、Claude のセッションを定刻に立てる形にした
（2026-08-11 設定・土日 07:00 JST）。

**07:00 にしたのは、10:07 の「買い目が届いていません」警報より 3 時間先に置くため。**
遅れても警報より先に着く。警報が鳴ったら、それは本当に届かなかったということ。

**カードの鮮度は日時で線を引く。「ファイルがある」は根拠にならない。**
2026-08-12（水）朝に 08-15 のカードを作れてしまった。中身は全15レース（確定時36）、
頭数が出走登録ベース（26頭。JRAは最大18頭）、オッズが `status:"yoso"` の予想オッズ。
**確定前の姿でも、ファイルとしては正常に見える。** 前夜の3本が全部落ちた週に
これを掴むと、暫定データで印を打つことになる。基準は「対象日の前日18:00 JST 以降」。
定時実行は3本とも前日夜〜当日未明なのでこの線を越え、週の半ばのものは越えない。

**Routine には手順を書かず、`docs/朝タスク手順.md` を読ませる。**
毎回まっさらなセッションが立つのでプロンプトは単体で完結する必要があるが、
手順の実体をプロンプトに書くと**リポジトリの手順書と二重管理になり、必ずずれる**。
プロンプトは「正本を読め」と分担線だけを持ち、中身は手順書に置く。

**地方は毎日回す。土日固定の仕組みでは重賞を1つも拾えない。**
2026年8月の12日間で地方の重賞・準重賞は16鞍あり、その大半が平日だった
（2026-08-12 の判断）。中央が土日だけなのに引きずられて土日で設計すると、
それが丸ごと落ちる。

**Routine が起こす新しいセッションには、リポジトリを明示しないと何もクローンされない。**
2026-08-12、地方の Routine の初回が自動発火・手動発火とも空振りした。原因は
プロンプトが「CLAUDE.md を読む」から始まっていたことで、真っさらなセッションには
CLAUDE.md どころかどのリポジトリかという情報すら無かった。セッション自身は
リポジトリ名を推測して勝手に `git clone` することはせず、正直に「特定できない」と
報告して終わっており、**判断は正しかった**。直すべきはプロンプト側だった。
対策として、プロンプトの先頭に `mcp__Claude_Code_Remote__add_repo` で
`owner: amanohara361, repo: keiba` を明示するステップ0を足した（中央側の
Routine にも同じ欠陥があったため、同時に直した）。**「ファイルを読め」で始まる
プロンプトは、読む前にファイルがそこにある根拠を持たない。**

**地方の検算は「対象がある日」だけ回す。**
地方は毎日どこかで開催しているが、重賞は毎日ない。無条件に毎日回すと
「朝の買い目が届いていません」が平日ほぼ毎日鳴り、**2026-08-01 を捕まえるための
警報が意味を失う**。`nar_card.py --gate` がカードを見て3つを区別する:
`run`（対象あり）/ `none`（取得できて該当なし・無音）/ `missing`（カードが無い・要通知）。
**`none` と `missing` を同じ扱いにしない。** 混ぜると、取得が壊れている日が
「今日は無い日」に紛れる。これは「取得できなかったことを静かに0件と出さない」の
同じ話で、今度は日単位で起きる。

---

## Cowork 側の制約

**Cowork のサンドボックスはマウント先のファイルを作成・更新できるが、削除できない。**
git はコミットのたびに `.git/index.lock` を作って消すので、Cowork では git が動かない。
2026-08-08 に残ったロックを消せず `Operation not permitted` で止まった。
~~→ 朝タスクはファイルを書くだけ。commit と push は Windows 側の `github_push.bat`
（タスクスケジューラで土日 08:00 / 12:00）。認証済みの Git Credential Manager を
使うのでトークンの受け渡しも要らない。~~
**2026-08-12 に撤回。** 下の「Cowork とクラウドを完全に分離する」を見よ。

**Cowork とクラウドを完全に分離する。`github_push.bat` は廃止（2026-08-12）。**
クラウド側に地方の Routine（毎日08:00 JST）が加わったことで、同じ日の
`data/bets/YYYY-MM-DD.json` を**3つの独立した書き手**（Cowork・中央 Routine・
地方 Routine）が触りうる状態になっていた。Cowork は書く前に `git pull` しない
ので、クラウド側が先に push した内容を知らずにローカルへ書き、`github_push.bat`
の `git pull --rebase` が同じファイルの衝突を起こしうる。衝突すれば push も失敗し、
**PC 上のリポジトリが rebase 途中の壊れた状態のまま残る**（次回実行時に
`git rebase --abort` が要る）。衝突しなくても、地方 Routine が「既存の races を
消さず追記する」設計で書いた内容を、その設計を知らない Cowork の書き込みが
丸ごと上書きして消しうる。

3つの独立した書き手を git 側で調停する（Cowork にも pull・マージ判断をさせる）
のではなく、**Cowork をクラウドから完全に切り離す**方を選んだ。Cowork の出力
（買い目txt・Excel・Drive・Gmail）はPC上で完結し、GitHubには一切触れない。
クラウド側（Routine・GitHub Actions）もCowork側のファイルを一切読まない。
お互いを見ないので、衝突がそもそも起こりようがない。

失うのは「同じ日に両方が予想を作ったとき、どちらを正とするか」の自動調停
（旧: source が cowork なら上書きしない）。今後は**その日どちらを見て馬券を
買うか、人が選ぶ**運用になる。GitHub の履歴だけでは全体が見渡しにくいという
理由もあり、Cowork 側のExcel・Gmailを人間が読む窓口として引き続き使う。

`github_push.bat` はファイルごと消さず、何もせずログを書いて終わる無害な
スタブに置き換えた（PC側のタスクスケジューラ登録を消し忘れても実害が
出ないようにする多重の保険）。

**ブラウザ経由でのコミットは採らない。**
この案件は「壊れやすい自動化が止まって機会を失う」ことへの対策なので、
毎週の必須経路にブラウザ自動化を置くと、セッション切れ・UI変更・Chrome未起動で
同じ失敗に戻る。

**アプリ内タスク同士は独立した保険にならない。**
2026-08-01 は朝タスクと14:30タスクが両方止まり、20:30過ぎに一斉起動した。
本命の保険はアプリの外にある `買い目確認.bat`（Windowsタスクスケジューラ）。

---

## 自動化の線引き

**「PRは作らない」は日次の買い目コミットだけの話。機能追加はブランチ＋PRにする
（2026-08-13 ユーザー承認済み）。**
`docs/地方競馬手順.md`・`docs/朝タスク手順.md`の「PRは作らない」は、
`nar_check.yml`/`check.yml`がmainしか読まないため、その日の買い目を
即座にmainへ入れる必要がある日次ルーティン向けの指示だった。
2026-08-13、この指示を機能追加（交流重賞のJRA所属馬をnetkeiba馬名検索で
補う仕組み）にもそのまま適用してmainへ直接コミットしてしまい、
「割と大きな仕様変更なのにレビューを経ていない」と指摘を受けた。
その回はそのまま残す（Actions上で本番動作も確認済みのため）が、
**次回以降、日次の買い目コミット以外の機能追加・仕様変更はブランチを切り
PRにする。**

**機械にやらせるのは算術と規則、人が持つのは印（判断）。**
第13章の規律（合成オッズ・期待値・印と買い目の整合）は機械的に判定できる。
一方で印を打つのは血統・展開・調教の読みで、いまは人と Cowork が持つ。

ただし**事実の照会は判断ではない**。馬場状態、各馬の重馬場成績、馬体重は
取ってきて並べるだけなので機械の担当（`conditions.py` / `form.py`）。
一度この線を引き間違えて「判断だから除外」としたが、それは誤りだった。

**規律に足すかどうかは人が決める。機械は件数を数えるところまで。**
週次レビューの `◎一極集中で全滅` は、検知はできるが規律には入れていない。
候補として件数だけ出し、10件たまったら人が判断する。

**基準値（合成オッズ3.0倍・期待値1.2・単勝4.0〜9.9倍）はサンプル10件まで変えない。**
一度の悔しさで基準を緩めると、規律が規律でなくなる。

**`bets.py` の JSON が予想側と検算側の唯一の接点。**
将来 Claude API が印を打つようになっても、同じ形を吐けば検算側は無変更で繋がる。
`source` にどこが作ったかを記録してあり、あとで精度を比較できる。

**印1つ1つに、根拠になった手順番号（`marks[].steps`）を仕込む（2026-08-12）。**
厳密な統計評価をするにはまだサンプルが足りないが、「外した時は手順のどこが
ズレたか振り返る」（第1章）という振り返りの単位は最初から手順1〜7で決まって
いる。的中率・回収率だけを記録すると、半年後に「そもそもどの手順が効いて
いないのか」を知る手段が無い。`note` の自由記述には理由が書かれているが、
構造化されていないため後から機械的に集計できない。番号だけなら書く側の
負担も小さく、後ろ向き研究（半年〜1年分たまってから振り返る）のコストは
今のうちに払っておいたほうが安い。過去分は遡って埋められないので、
効果が出るのはこれからの蓄積分から。任意項目にしたのは、無理に埋めて
不正確なタグが付くより、無い方がまだ安全なため。

**週次レビューに「配当の偏り」を足す（2026-08-12・普通のチャットでの相談を受けて）。**
回収率は少数の高配当に強く引っ張られる（分散が大きい）ので、`回収率（規律適用後）`
だけを見ていると、実力ではなく運が良かった週を実力と誤読しかねない。的中の
配当の中央値・最小〜最大・上位1件／3件を除いた回収率を並べて出す。
**上位N件を除くときも賭け金は減らさない**（その馬券を買ったこと自体は事実
なので、除くのは払戻の側だけ）。判定文は書かず数字だけ出す（`review.py`
の既存方針を維持）。置き場所は「規律は収支に効いたか」のすぐ下
（.md・メールの両方）——その数字の解釈に直結する注記だから隣に置く。

**地方は別リポジトリにしない。分けるのはリポジトリではなく時間軸。**
`予想メソッド.md` は第11章と第14章に地方の条文を既に持っており、
`discipline.py` の規律・`bets.py` の契約・`check.py`・`review.py` は
中央と地方で完全に同じものが通る。別リポにすると `予想メソッド.md` と
`検証ノート.md` が2か所になり、**それは 2026-08-07 に `data_jra/` を
20レース分失ったのと同じ形**（下の「同じ名前のファイルが2か所にあると壊れる」）。
分けたのはワークフローと開催カレンダーだけで、`nar_card.yml` / `nar_check.yml` /
`data/cards/nar/` を別に持ち、買い目ファイルは1本のまま `org` で判別する。

**主催者は `org` で判別する。race_id の数字から推測しない。**
中央（netkeiba形式）も地方（`YYYYMMDD+場コード+R`）も12桁の数字で、
桁を見ても区別が付かない。`org` が無い過去のファイルは中央として読む。

**地方の1次スクリーニングは推測を1つも含まない。**
公式CSVの `競走種類名称` に主催者自身が 重賞／準重賞／特別／普通 と書いている。
中央でアイコン番号を推測して浜名湖特別（2勝クラス）をG2に化けさせた事故が、
地方では構造的に起きない。**基準を自分で作らないことがそのまま精度になる。**

**交流重賞の中央馬は「近走0走」が正常。休み明けと同じ形で出さない。**
NAR の公式CSVには地方のレースしか入らないので、JRA所属馬の近走は必ず空になる。
`調教師所属` が `JRA` になっているので判別でき、`belongs_jra` と
`last_run.note` の文言を分けてある。**同じ0走で意味が真逆**であり、
混ぜると第2章の減点方式が逆向きに働く。
2026-08-11 クラスターカップJpnIII は10頭中4頭が JRA 所属だった。

**印は7ステップ適用時（朝）に確定するが、買い目の組み立ては実オッズが
確定してから（直前検算）に一本化する（2026-08-14 ユーザー承認）。**
朝07:00の時点はJRAでもオッズが未確定のことがあり、地方はナイターで発走が
16:30〜21:00台のため朝は「予想オッズ（発売前）」しか無いことが特に多い。
第13章の基準（合成オッズ3.0倍・期待値1.2）は入力にオッズを使うので、
暫定オッズで買い目まで固めると規律が本来の意味で機能しない。
きっかけは「印と買い目は独立のはずだが、買い目もオッズが出る前に決めている
のでは」というユーザーからの指摘で、実際そのとおりだった。

- 朝タスクは `marks`・`partners`（中穴候補：印は無いが分析シート上でプラス
  材料があった馬。馬番＋短い理由）・`subjective_hit_rate` までを書く。
  `bets`・`confidence` は書かない。
- 直前検算（`check.py`）が実オッズを取ったあと `bet_builder.py` を呼び、
  相手プール（○→▲→△→中穴候補の優先順）に対して第13章F案の優先順位
  （①相手を絞る→②ワイドへ→③見送り）を機械的にたどって買い目と勝負度を
  組み立て、`data/bets/YYYY-MM-DD.json` へ書き戻す。毎回の検算で最新の
  実オッズを使って組み直す（結果はロックしない。実オッズ優先の原則と同じ
  理由）。
- **相手プールを常に印の優先順で並べたことで、印の上位馬（◎○）が相手から
  外れ下位の印だけが残る、という2026-08-02クイーンSの事故は構造的に
  起こらなくなった。**
- **各段は、相手全員ぶんのオッズが揃わなければ不成立とする（2026-08-14、
  実装直後にユーザーが指摘して即日修正）。** 当初案は「引けたオッズだけで
  部分的に組み、矛盾は discipline.check_matched_marks が捕まえる」だった。
  しかしこの安全網は「上位印が買い目に無く、かつ下位印が買い目にある」
  形しか検知できない。無印の中穴候補（`partners`）だけにオッズがあり
  印馬（◎○）に無い場合、買い目に下位印が1つも残らないため
  check_matched_marksもすり抜ける——検知できない、より悪い形の同じ事故が
  作れてしまうことが分かった。実オッズ優先の原則に立ち返り、
  一部でも欠けた段は使わず次の段（絞り込み）へ進む方式に直した。
  この結果、discipline.py の合成オッズ・期待値・印の矛盾チェックは
  bet_builder の出力に対して構造的に違反しなくなり、通常経路で実際に
  発火しうるBLOCKは「発走済み」だけになった。安全網としては
  `make_bets.py` での手組みなど、bet_builder を経由しない買い目に対して
  引き続き効く。
- **勝負度A/Bの最終線引きは期待値で行う：1.5以上をA、1.2以上1.5未満をB。**
  旧来の「合成オッズに妙味がある」という定性的な基準を、直前検算が機械で
  判定できる形に数値化した。1.5という値自体は検証で緩和・変更が承認される
  まで変えない（第13章の他の基準値と同じ扱い）。
- **`confidence` 未設定時の既定値を `B` から `None` に変えた（2026-08-14、
  ユーザーがメールの言い回しを指摘して発覚）。** 実例：2026-08-14 摂津盃は
  19:34:46（発走19:55の20分前）に合成4.16倍で規律をクリアした買い目が
  確定していたが、20:23（発走後）の実行が「発注を止めました／購入できません」
  と表示した。過去の検算記録（`data/checks/`）を遡ると、実際には49分前に
  評価済みだったと分かる——が、メール本文だけを見るとその区別がつかない。
  原因は `confidence` の既定値が `B` だったこと。直前検算が一度も評価
  できないまま発走した場合（2026-08-01の事故そのもの）と、評価済みで
  `B`/`C` になった場合が、どちらも見た目上「`B` で発走」になり
  区別不能だった。`None` を「未評価」を表す独立の状態にしたことで、
  `check_already_started` は `confidence is None` のときだけ真のBLOCK
  （「発走までに一度も評価できませんでした」）を出し、それ以外
  （`A`/`B`/`C` のいずれか、`C`＝見送りを含む）は発走後の実行でもBLOCKに
  しない（直近の検算結果がそのまま最終形なので、単に間に合わなかっただけの
  無害な再確認として扱う）。

**朝タスクのpush直後に、check.yml（NARはnar_check.yml）を1回手動起動する
（2026-08-15 ユーザー指摘）。** 2026-08-15、中京2R（3歳未勝利・10:20発走）が
「発走までに一度も評価できませんでした」でBLOCKした。10:07の定時実行が
間に合わなかったため——定時実行は85〜119分の遅延を実測しており、10:20発走の
レースにはそもそも間に合う保証が無い。手動起動（workflow_dispatch）は定時
実行と違って遅延しない（数秒で始まる）ので、朝タスクが印・中穴候補を
push した直後にこれを1回挟み、その時点で市場が開いているレースは
その場で買い目まで確定させる。以降の定時実行は通常どおり実オッズで
組み直していく。`no_email: true` にして、通知は定時実行に任せる
（この1回は買い目を早く確定させるためだけの実行）。

**買い目が無いのに「規律をクリアしています」と出るのは誤読を招く
（2026-08-15 ユーザー指摘）。** 勝負度Cで買い目が0件のとき、discipline
はBLOCK・WARNのどちらも出さない（INFOのno_betsが1件あるだけ）ため、
`format_verdict`の「BLOCK/WARNが無ければクリアと表示する」というロジックが
そのまま「✓ 第13章の規律をすべてクリアしています」を出していた。
これは「買い目が規律に合格した」ように読めるが、実際には買い目自体が
無いので何も検証していない。`race.bets`が空のときは「－ 見送り（買い目なし）」
に出し分けるよう直した。**`report_html.py`（GitHub Pages・`docs/index.html`）
にも同じ構造の分岐が独立に存在しており、同じ理由で同じ修正を入れた。**
メールの文面（check.py）とページの文面（report_html.py）は別コードなので、
文面の修正は両方を確認すること。

**bet_builderの説明文はrace.noteに書かない。verdict.bet_noteという毎回
使い捨ての場所に置く（2026-08-15、GitHub Pagesのメモ欄に同じ「見送り」
文言が3回連結されているのをユーザーが発見）。** `_build_race_bets`が
`race.note = f'{race.note} / {note}' if race.note else note`という形で
書いていたため、1日に複数回走る検算のたびに追記され、無限に伸びていた。
race.noteは朝タスクが書いた分析メモであり、data/bets/に永続化される
フィールドなので、検算のたびに変わる一時的な情報を書き込む場所ではない。
`RaceVerdict`に`bet_note`属性（既定None）を足し、check.py側で検算のたびに
その回の説明文を代入するだけにした（discipline.pyはbet_builderを知らない
ので、コンストラクタ引数にはしない）。check.py・report_html.pyの両方で
race.note（朝タスクのメモ）とは別枠の「買い目メモ」として表示する。

---

## データの扱い

**`data_jra/` を手で上げない。Actions が管理する。**
2026-08-07 に手動アップロードでクラウド側の完全なデータ（各場12レース）を
部分データ（3レース・1レース）で上書きし、20レース分を失った。

**同じ名前のファイルが2か所にあると、こうやって壊れる。**
`CLAUDE.md` `予想メソッド.md` `検証ノート.md` `jra_bias.py` は
**keiba-repo 側を正**とし、作業前に `git pull` する。

**バイアスは1レースだけ見て断定しない。同日横比較まで行う。**
2026-08-01 の札幌11Rだけを見ると外差しに見えたが、同日7Rは先行勢が1〜3着を独占していた。
レースごとに逆を向いていたら、それは馬場ではなく展開。
実際、8/2 のデータを完全な形で取り直したら判定が「外伸び」から「イン前」へ反転した。
先の答えが間違っていた原因はロジックではなくサンプル数だった。

**確定した着順は二度と変わらないので、一度取ったら保存して取り直さない**
（`data/results/`）。レビューを何度流し直しても負荷が増えず、
テストもネットワーク無しで実データを使える。

**実在レースの着順を手で書き起こしてテストに埋めない。**
書き間違いがそのまま「実績」として残る。着順が要るテストは架空のフィクスチャを使い、
実データは着順の要らない検証（検算記録の読み方など）に限る。

---

## ファイル形式（Windows 側）

**`.bat` と `.ps1` は ASCII のみ。改行は CRLF。**
cmd.exe は bat を OEM コードページ（cp932）で解釈するので、BOMなしUTF-8の日本語
リテラルは解析時点で化ける。PowerShell 5.1 も BOMなし `.ps1` を同様に読む。
LF のまま保存すると `goto` のラベルが解決できず、**メッセージも出さずウィンドウが消える**。
`.py` は対象外（Python3 は UTF-8 既定）。

- 変換：`sed -i 's/\r*$/\r/' *.bat`
- 検証：`grep -c $'\r' f.bat` と `wc -l < f.bat` が一致すれば CRLF
- 検証：`grep -nP '[^\x00-\x7F]' f.ps1` が無出力なら ASCII のみ
- 括弧ブロック内の echo に `)` を書かない（ブロックが途中で閉じる）
